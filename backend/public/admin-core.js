const API='/api';
const PAGE=5;
const $=id=>document.getElementById(id);
const tokenKey='abobby_admin_token';
const userKey='abobby_admin_user';
let bookings=[],rooms=[],contacts=[],bPage=1,cPage=1;
const token=()=>localStorage.getItem(tokenKey);
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const money=n=>'₦'+Number(n||0).toLocaleString();
const d=v=>v?new Date(v).toLocaleDateString('en-GB',{day:'2-digit',month:'short',year:'numeric'}):'-';
const note=(txt,label)=>{const m=String(txt||'').match(new RegExp(label+':\\s*([^|]+)','i'));return m?m[1].trim():''};
const imgList=v=>Array.isArray(v)?v.filter(Boolean):(typeof v==='string'?v.split(',').map(x=>x.trim()).filter(Boolean):[]);
async function api(path,opt={}){const r=await fetch(API+path,{...opt,headers:{'Content-Type':'application/json',...(token()?{Authorization:'Bearer '+token()}:{}),...(opt.headers||{})}});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.message||data.error||'Request failed');return data}
function alertBox(msg,type='success'){const box=$('adminAlert');if(!box)return alert(msg);box.textContent=msg;box.className='admin-alert '+type;setTimeout(()=>{box.textContent='';box.className='admin-alert'},3500)}
function status(el,msg,type=''){el.textContent=msg;el.className=('status '+type).trim()}
function showLogin(){$('loginSection').classList.remove('hidden');$('dashboardSection').classList.add('hidden');$('logoutBtn').classList.add('hidden')}
function showDash(){const u=JSON.parse(localStorage.getItem(userKey)||'null');$('loginSection').classList.add('hidden');$('dashboardSection').classList.remove('hidden');$('logoutBtn').classList.remove('hidden');$('adminUser').textContent=u?`${u.name} (${u.email})`:'Admin'}
function ensureContact(){if($('contactsTable'))return;const s=document.createElement('section');s.id='contactSection';s.className='card';s.innerHTML=`<div class="section-title"><div><h2>Contact Messages</h2><span class="admin-section-note">Read customer enquiries, search messages, and send replies from one clean workspace.</span></div><button id="refreshContactsBtn">Refresh Messages</button></div><div id="contactMiniStats" class="section-mini-stats"></div><div class="toolbar"><div><label>Search messages</label><input id="contactSearch" placeholder="Name, email, message..."></div><div><label>Status</label><select id="contactStatus"><option value="all">All Messages</option><option value="new">New Only</option><option value="replied">Replied Only</option></select></div></div><div class="table-wrap"><table><thead><tr><th>Customer</th><th>Message</th><th>Status</th><th>Reply</th></tr></thead><tbody id="contactsTable"></tbody></table></div><div class="pager"><span id="contactInfo" class="muted"></span><div><button id="contactPrev" class="secondary">Previous</button><button id="contactNext" class="secondary">Next</button></div></div>`;$('roomEditorCard').parentNode.insertBefore(s,$('roomEditorCard'));$('refreshContactsBtn').onclick=loadContacts;$('contactSearch').oninput=()=>{cPage=1;renderContacts()};$('contactStatus').onchange=()=>{cPage=1;renderContacts()};$('contactPrev').onclick=()=>{cPage=Math.max(1,cPage-1);renderContacts()};$('contactNext').onclick=()=>{cPage++;renderContacts()}}
function fBookings(){const q=($('bookingSearch')?.value||'').toLowerCase().trim(),st=$('bookingStatusFilter')?.value||'all',pay=$('bookingPaymentFilter')?.value||'all';return bookings.filter(b=>(!q||`${b.reference} ${b.full_name} ${b.email} ${b.phone} ${b.room_name} ${b.room_type}`.toLowerCase().includes(q))&&(st==='all'||b.status===st)&&(pay==='all'||b.payment_status===pay))}
function renderBookings(){const list=fBookings(),pages=Math.max(1,Math.ceil(list.length/PAGE));bPage=Math.min(Math.max(1,bPage),pages);const start=(bPage-1)*PAGE,rows=list.slice(start,start+PAGE);const mini=$('bookingMiniStats');if(mini){mini.innerHTML=`<span class="section-mini-stat">Total: ${bookings.length}</span><span class="section-mini-stat">Confirmed: ${bookings.filter(b=>b.status==='confirmed').length}</span><span class="section-mini-stat">Pending: ${bookings.filter(b=>b.status==='pending').length}</span><span class="section-mini-stat">Cancelled: ${bookings.filter(b=>b.status==='cancelled').length}</span>`}$('bookingsTable').innerHTML=rows.length?rows.map(b=>{const wallet=note(b.payment_note,'Selected Bank/Wallet')||b.transfer_bank||'-',nar=note(b.payment_note,'Unique Narration')||'-',proof=b.payment_proof?`<a href="${b.payment_proof}" target="_blank"><button class="secondary payment-proof-btn">View Proof</button></a>`:'<span class="muted">No proof uploaded</span>';let act='';if(b.payment_status!=='paid')act+=`<button class="success booking-paid-btn" onclick="updateBooking(${b.id},'confirmed','paid')">Mark Paid</button>`;if(b.status!=='confirmed')act+=`<button class="warning booking-confirm-btn" onclick="updateBooking(${b.id},'confirmed','${b.payment_status}')">Confirm</button>`;if(b.status==='cancelled')act+=`<button class="warning booking-confirm-btn" onclick="updateBooking(${b.id},'pending','${b.payment_status}')">Restore</button>`;else act+=`<button class="danger booking-cancel-btn" onclick="updateBooking(${b.id},'cancelled','${b.payment_status}')">Cancel</button>`;act+=`<button class="danger booking-delete-btn" onclick="deleteBooking(${b.id})">Delete</button>`;const pay=b.payment_method==='bank_transfer'?`<div class="payment-box"><span class="pill ${esc(b.payment_status)}">${esc(b.payment_status)}</span><div class="payment-method-title">Transfer</div><span class="muted">Wallet: ${esc(wallet)}</span><br><span class="muted">Narration: ${esc(nar)}</span><br>${proof}</div>`:`<div class="payment-box"><span class="pill ${esc(b.payment_status)}">${esc(b.payment_status)}</span><div class="payment-method-title">${esc(b.payment_method||'Paystack/None')}</div></div>`;return `<tr><td><span class="booking-ref">${esc(b.reference)}</span></td><td><div class="booking-guest-card"><span class="contact-name-strong">${esc(b.full_name)}</span><span class="booking-meta">${esc(b.email)}</span><span class="booking-meta">Phone: ${esc(b.phone||'-')}</span><span class="booking-meta">Gender: ${esc(b.gender||'-')}</span><span class="booking-meta">Next of Kin: ${esc(b.next_of_kin_name||'-')}</span></div></td><td><strong>${esc(b.room_name||b.room_type||'-')}</strong></td><td><div class="booking-date-box"><b>Check-in:</b> ${d(b.check_in)}<br><b>Check-out:</b> ${d(b.check_out)}</div></td><td><span class="booking-total">${money(b.total)}</span></td><td><span class="pill ${esc(b.status)}">${esc(b.status)}</span></td><td>${pay}</td><td><div class="booking-action-stack">${act}</div></td></tr>`}).join(''):'<tr><td colspan="8">No bookings found.</td></tr>';$('bookingPageInfo').textContent=`Showing ${list.length?start+1:0}-${Math.min(start+PAGE,list.length)} of ${list.length} bookings | Page ${bPage}/${pages}`;$('bookingPrevBtn').disabled=bPage<=1;$('bookingNextBtn').disabled=bPage>=pages}
async function loadBookings(){$('bookingsTable').innerHTML='<tr><td colspan="8">Loading bookings...</td></tr>';const data=await api('/bookings');bookings=data.bookings||[];$('bookingCount').textContent=bookings.length;$('paidCount').textContent=bookings.filter(b=>b.payment_status==='paid').length;renderBookings()}
function fContacts(){const q=($('contactSearch')?.value||'').toLowerCase().trim(),st=$('contactStatus')?.value||'all';return contacts.filter(c=>(!q||`${c.name} ${c.email} ${c.phone} ${c.subject} ${c.message}`.toLowerCase().includes(q))&&(st==='all'||c.status===st))}
function renderContacts(){ensureContact();const list=fContacts(),pages=Math.max(1,Math.ceil(list.length/PAGE));cPage=Math.min(Math.max(1,cPage),pages);const start=(cPage-1)*PAGE,rows=list.slice(start,start+PAGE);const mini=$('contactMiniStats');if(mini){mini.innerHTML=`<span class="section-mini-stat">Messages: ${contacts.length}</span><span class="section-mini-stat">New: ${contacts.filter(c=>(c.status||'new')==='new').length}</span><span class="section-mini-stat">Replied: ${contacts.filter(c=>c.status==='replied').length}</span>`}$('contactsTable').innerHTML=rows.length?rows.map(c=>`<tr><td><div class="contact-customer-card"><span class="contact-name-strong">${esc(c.name)}</span><span class="contact-meta">${esc(c.email)}</span><span class="contact-meta">${esc(c.phone||'No phone')}</span></div></td><td><div class="contact-message-box"><strong>${esc(c.subject||'Contact Message')}</strong><div class="contact-message-text">${esc(c.message)}</div>${c.admin_reply?`<div class="admin-reply-preview"><b>Admin Reply:</b><br>${esc(c.admin_reply)}</div>`:''}</div></td><td><span class="pill ${esc(c.status||'new')}">${esc(c.status||'new')}</span></td><td><div class="contact-reply-panel"><textarea id="reply-${c.id}" rows="3" placeholder="Type admin reply...">${esc(c.admin_reply||'')}</textarea><button class="success reply-btn" onclick="replyContact(${c.id})">Send Reply</button></div></td></tr>`).join(''):'<tr><td colspan="4">No contact messages found.</td></tr>';$('contactInfo').textContent=`Showing ${list.length?start+1:0}-${Math.min(start+PAGE,list.length)} of ${list.length} messages | Page ${cPage}/${pages}`;$('contactPrev').disabled=cPage<=1;$('contactNext').disabled=cPage>=pages}
async function loadContacts(){ensureContact();$('contactsTable').innerHTML='<tr><td colspan="4">Loading messages...</td></tr>';const data=await api('/contact');contacts=data.messages||[];renderContacts()}
async function loadRooms(){const data=await api('/rooms');rooms=data.rooms||[];$('roomCount').textContent=rooms.length;const mini=$('roomMiniStats');if(mini){mini.innerHTML=`<span class="section-mini-stat">Rooms: ${rooms.length}</span><span class="section-mini-stat">Available: ${rooms.filter(r=>r.available).length}</span><span class="section-mini-stat">Disabled: ${rooms.filter(r=>!r.available).length}</span>`}$('roomsTable').innerHTML=rooms.map(r=>{const img=imgList(r.images)[0];return `<tr><td><div class="room-image-box">${img?`<img class="thumb" src="${esc(img)}">`:'<div class="empty-image">No Img</div>'}</div></td><td><div class="room-title-card"><span class="room-name-strong">${esc(r.name)}</span><span class="room-meta">${esc(r.description||'No description').slice(0,90)}${String(r.description||'').length>90?'...':''}</span></div></td><td><span class="pill">${esc(r.type)}</span></td><td><span class="booking-total">${money(r.price)}</span></td><td>${r.capacity||'-'} guest(s)</td><td><span class="pill ${r.available?'available':'unavailable'}">${r.available?'Available':'Unavailable'}</span></td><td><div class="room-action-stack"><button class="room-edit-btn" onclick="editRoom(${r.id})">Edit</button><button class="warning room-disable-btn" onclick="toggleRoom(${r.id})">${r.available?'Disable':'Enable'}</button><button class="danger room-delete-btn" onclick="deleteRoom(${r.id})">Delete</button></div></td></tr>`}).join('')||'<tr><td colspan="7">No rooms found.</td></tr>'}
function roomData(){return{name:$('roomName').value.trim(),type:$('roomType').value,price:Number($('roomPrice').value),capacity:Number($('roomCapacity').value),description:$('roomDescription').value.trim(),amenities:$('roomAmenities').value.split(',').map(x=>x.trim()).filter(Boolean),images:$('roomImages').value.split(',').map(x=>x.trim()).filter(Boolean),available:$('roomAvailable').value==='true'}}
function resetRoom(){$('roomForm').reset();$('roomId').value='';$('roomCapacity').value=2;$('roomAvailable').value='true';$('roomFormTitle').textContent='Add Room';$('editBanner').classList.add('hidden');$('cancelEditBtn').classList.add('hidden');$('imagePreview').innerHTML=''}
window.updateBooking=async(id,status,payment_status)=>{try{await api(`/bookings/${id}`,{method:'PATCH',body:JSON.stringify({status,payment_status})});alertBox('Booking updated successfully');await loadBookings()}catch(e){alertBox(e.message,'error')}};
window.deleteBooking=async id=>{if(!confirm('Delete this booking permanently?'))return;try{await api(`/bookings/${id}`,{method:'DELETE'});alertBox('Booking deleted');await loadBookings()}catch(e){alertBox(e.message,'error')}};
window.replyContact=async id=>{const replyMessage=$(`reply-${id}`).value.trim();if(!replyMessage)return alertBox('Type a reply first','error');try{await api(`/contact/${id}/reply`,{method:'POST',body:JSON.stringify({replyMessage})});alertBox('Reply saved. Email will send if active.');await loadContacts()}catch(e){alertBox(e.message,'error')}};
window.editRoom=id=>{const r=rooms.find(x=>Number(x.id)===Number(id));if(!r)return;$('roomId').value=r.id;$('roomName').value=r.name||'';$('roomType').value=r.type||'standard';$('roomPrice').value=r.price||'';$('roomCapacity').value=r.capacity||2;$('roomDescription').value=r.description||'';$('roomAmenities').value=Array.isArray(r.amenities)?r.amenities.join(', '):'';$('roomImages').value=Array.isArray(r.images)?r.images.join(', '):'';$('roomAvailable').value=String(Boolean(r.available));$('roomFormTitle').textContent=`Edit Room #${r.id}`;$('editBanner').textContent=`Editing: ${r.name}`;$('editBanner').classList.remove('hidden');$('cancelEditBtn').classList.remove('hidden');if(window.showAdminTab)window.showAdminTab('add-room');else $('roomEditorCard').scrollIntoView({behavior:'smooth'})};
window.toggleRoom=async id=>{const r=rooms.find(x=>Number(x.id)===Number(id));if(!r)return;try{await api(`/rooms/${id}`,{method:'PUT',body:JSON.stringify({...r,available:!r.available})});alertBox('Room updated');await loadRooms()}catch(e){alertBox(e.message,'error')}};
window.deleteRoom=async id=>{if(!confirm('Delete this room permanently?'))return;try{await api(`/rooms/${id}`,{method:'DELETE'});alertBox('Room deleted');await loadRooms()}catch(e){alertBox(e.message,'error')}};
async function loadAll(){showDash();ensureContact();upgradeStaticSections();await Promise.all([loadRooms(),loadBookings(),loadContacts()])}
function upgradeStaticSections(){const bookingCard=[...document.querySelectorAll('#dashboardSection > .card')].find(c=>c.querySelector('h2')?.textContent.toLowerCase().includes('bookings'));if(bookingCard&&!$('bookingMiniStats')){bookingCard.querySelector('.section-title h2').insertAdjacentHTML('afterend','<span class="admin-section-note">Control reservations, verify payments, confirm stays, cancel wrong records, or delete test bookings.</span>');bookingCard.querySelector('.section-title').insertAdjacentHTML('afterend','<div id="bookingMiniStats" class="section-mini-stats"></div>')}const roomCard=[...document.querySelectorAll('#dashboardSection > .card')].find(c=>c.querySelector('h2')?.textContent.toLowerCase().includes('room management'));if(roomCard&&!$('roomMiniStats')){roomCard.querySelector('.section-title h2').insertAdjacentHTML('afterend','<span class="admin-section-note">Manage room images, prices, capacity, status, and availability from one section.</span>');roomCard.querySelector('.section-title').insertAdjacentHTML('afterend','<div id="roomMiniStats" class="section-mini-stats"></div>')}if($('roomEditorCard')&&!$('roomFormHelper')){$('roomFormTitle').insertAdjacentHTML('afterend','<span id="roomFormHelper" class="admin-section-note">Create a new room or edit an existing room. Use short descriptions and clear room images.</span>');$('roomName').insertAdjacentHTML('afterend','<span class="form-helper-text">Example: Deluxe King Room, Executive Suite Plus.</span>');$('roomImages').insertAdjacentHTML('afterend','<span class="form-helper-text">Paste image URLs separated with commas or upload one small image below.</span>');$('roomAmenities').insertAdjacentHTML('afterend','<span class="form-helper-text">Example: WiFi, AC, TV, Mini Bar, Jacuzzi.</span>')}}
$('loginForm').onsubmit=async e=>{e.preventDefault();$('loginBtn').disabled=true;status($('loginStatus'),'Logging in...');try{const data=await api('/auth/login',{method:'POST',body:JSON.stringify({email:$('email').value.trim(),password:$('password').value})});localStorage.setItem(tokenKey,data.token);localStorage.setItem(userKey,JSON.stringify(data.user));await loadAll()}catch(err){status($('loginStatus'),err.message,'error')}finally{$('loginBtn').disabled=false}};
$('logoutBtn').onclick=()=>{localStorage.removeItem(tokenKey);localStorage.removeItem(userKey);showLogin()};
$('refreshBookingsBtn').onclick=loadBookings;$('refreshRoomsBtn').onclick=loadRooms;['bookingSearch','bookingStatusFilter','bookingPaymentFilter'].forEach(id=>$(id).oninput=$(id).onchange=()=>{bPage=1;renderBookings()});$('bookingPrevBtn').onclick=()=>{bPage=Math.max(1,bPage-1);renderBookings()};$('bookingNextBtn').onclick=()=>{bPage++;renderBookings()};$('newRoomBtn').onclick=resetRoom;$('cancelEditBtn').onclick=resetRoom;$('roomImages').oninput=()=>{$('imagePreview').innerHTML=$('roomImages').value.split(',').map(x=>x.trim()).filter(Boolean).map(u=>`<img class="thumb" src="${esc(u)}">`).join('')};$('roomImageUpload').onchange=e=>{const f=e.target.files?.[0];if(!f)return;if(f.size>750000){alertBox('Image too large. Use below 750KB','error');return}const rd=new FileReader();rd.onload=()=>{$('roomImages').value=($('roomImages').value.trim()?$('roomImages').value+', ':'')+rd.result;$('roomImages').dispatchEvent(new Event('input'))};rd.readAsDataURL(f)};$('roomForm').onsubmit=async e=>{e.preventDefault();const id=$('roomId').value;try{await api(id?`/rooms/${id}`:'/rooms',{method:id?'PUT':'POST',body:JSON.stringify(roomData())});alertBox(id?'Room updated':'Room added');resetRoom();await loadRooms();if(window.showAdminTab)window.showAdminTab('rooms')}catch(err){alertBox(err.message,'error')}};
if(token())loadAll().catch(e=>{alertBox(e.message,'error');showLogin()});else showLogin();